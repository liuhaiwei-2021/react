/** @format */

const initialState = {
    inputValue: 'writesomething',
    list:["clean house", "buy some fruit", "watch TV"]
    
};
const reducer=(state = initialState, action) => {
  return state;
};
export default reducer
