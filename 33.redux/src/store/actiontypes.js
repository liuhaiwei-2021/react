/** @format */

const actiontypes = () => {
  return {
    counter: {
      increment: "INCREMENT",
      decrement: "DECREMENT",
    },
    quantity: {
      increment: "INCREMENT_QUANTITY",
      decrement: "DECREMENT_QUANTITY",    
    }
  };
};

export default actiontypes